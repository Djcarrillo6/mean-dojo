const { rainbow } = require("colors");

const matrix = [
  [0, 3, 1, 1],
  [2, 0, 0, 4],
  [1, 5, 3, 1]
];


function coinCounter(matrix, i, j) {
  if(i < 0 || i > matrix.length || j < 0 || j > matrix[0].length || matrix[i][j] === -1) return 0;
  else if(x === 0 && y === 0) {
    return 0;
  };

  coinCounter()
};